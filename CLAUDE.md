# CLAUDE 兼容入口

这个文件只保留给兼容读取 `CLAUDE.md` 的代理。

当前项目唯一长期协作说明请读：

- `AGENTS.md`

补充上下文按这个顺序看：

1. `AGENTS.md`
2. `memory/project-structure-reference.md`
3. `docs/homepage-architecture.md`

注意：

- 当前首页是新版沉浸式结构，不是旧 `bento-grid`
- `data.js` 是站点导航唯一数据源
- 历史备份和旧概念资产已经不保留在工作树里，需要时请查 Git 历史
